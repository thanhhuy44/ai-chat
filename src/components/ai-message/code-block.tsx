/**
 * Fenced code block renderer.
 *
 * Uses streamdown's built-in CodeBlock component which handles:
 * - Syntax highlighting (Shiki)
 * - Copy button
 * - Download button
 * - Line numbers
 * - Streaming (incomplete) states
 */
export { CodeBlock } from 'streamdown'